import React from 'react';

import './header.css';

// common header component
const Header = () => (
    <div className="header-container">
        <h1>JObSite</h1>
    </div>
);

export default Header;
